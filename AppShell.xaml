<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="financeTracker.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:financeTracker"
    Shell.FlyoutBehavior="Flyout"
    Title="Finance Tracker">

    <ShellContent Title="Dashboard" ContentTemplate="{DataTemplate local:DashboardPage}" />
    <!-- <ShellContent Title="Add Transaction" ContentTemplate="{DataTemplate local:AddTransactionPage}" /> -->
    <!-- <ShellContent Title="Transactions" ContentTemplate="{DataTemplate local:ViewTransactionsPage}" /> -->

    <!-- <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" /> -->

</Shell>
