<?xml version="1.0" encoding="utf-8" ?>

<ContentView
    x:Class="trackr.Controls.BottomSheet"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="Root"
    IsVisible="False"
    HorizontalOptions="Fill"
    VerticalOptions="Fill">

    <Grid>

        <!-- Background -->
        <BoxView
            BackgroundColor="{StaticResource TranslucentBackground}"
            HorizontalOptions="Fill"
            VerticalOptions="Fill"
            InputTransparent="False">

            <BoxView.GestureRecognizers>

                <TapGestureRecognizer
                    Tapped="BackgroundTapped"/>

            </BoxView.GestureRecognizers>

        </BoxView>

        <!-- Sheet -->
        <Border
            x:Name="Sheet"
            VerticalOptions="End"
            HorizontalOptions="Fill"
            Margin="0,0,0,-35"
            Padding="25"
            BackgroundColor="{AppThemeBinding Light={StaticResource SecondaryBackgroundLight}, Dark={StaticResource SecondaryBackgroundDark}}"
            StrokeThickness="0">

            <Border.StrokeShape>

                <RoundRectangle
                    CornerRadius="50"/>

            </Border.StrokeShape>

            <VerticalStackLayout
                Spacing="10">

                <!-- Handle -->
                <BoxView
                    WidthRequest="40"
                    HeightRequest="5"
                    HorizontalOptions="Center"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray400}, Dark={StaticResource Gray600}}"
                    CornerRadius="5"/>

                <!-- Header -->
                <Grid>

                    <Grid.ColumnDefinitions>

                        <ColumnDefinition
                            Width="*"/>

                        <ColumnDefinition
                            Width="Auto"/>

                    </Grid.ColumnDefinitions>

                    <Label
                        Text="{Binding Title, Source={x:Reference Root}}"
                        VerticalOptions="Center"
                        HorizontalOptions="Start"
                        FontSize="18"
                        FontAttributes="Bold"
                        TextColor="{AppThemeBinding Light={StaticResource PrimaryTextLight}, Dark={StaticResource PrimaryTextDark}}"/>

                    <Button
                        WidthRequest="20"
                        HeightRequest="20"
                        CornerRadius="10"
                        VerticalOptions="Center"
                        HorizontalOptions="End"
                        Text="✕"
                        BackgroundColor="{AppThemeBinding Light={StaticResource PrimaryAccentPurpleLight}, Dark={StaticResource PrimaryAccentPurpleDark}}"
                        TextColor="{AppThemeBinding Light={StaticResource PrimaryTextLight}, Dark={StaticResource PrimaryTextDark}}"
                        Clicked="CloseClicked"/>

                </Grid>

                <!-- Custom content goes here -->
                <ScrollView>

                    <ContentView
                        x:Name="ContentHost"/>

                </ScrollView>

            </VerticalStackLayout>

        </Border>

    </Grid>

</ContentView>